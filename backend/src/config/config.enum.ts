export enum Configs {
  guardarian = 'guardarian',
  app = 'app',
}

export enum Environments {
  development = 'development',
  staging = 'staging',
  production = 'production',
  testing = 'testing',
}
